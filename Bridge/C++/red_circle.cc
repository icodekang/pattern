#include "red_circle.h"

void RedCircle::draw_circle(int radius, int x, int y)
{
    std::cout << "drawing circle[ color: red, radius: " << radius << ", x: " << x + ", y: " << y << "]" << std::endl; 
}