#include "green_circle.h"

void GreenCircle::draw_circle(int radius, int x, int y)
{
    std::cout << "drawing circle[ color: green, radius: " << radius << ", x: " << x + ", y: " << y << "]" << std::endl; 
}